[build]
  builder = "nixpacks"
  buildCommand = "pip install -r requirements.txt"

[deploy]
  startCommand = "python bot.py"
  healthcheckPath = "/health"

[service]
  name = "cryptopulse-ai"
  maxMemory = "900MB"
